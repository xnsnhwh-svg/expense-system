# 企业财务智能报销与审核编排系统

> 项目类型：AI Agent 应用系统
> 技术栈：FastAPI + React + CrewAI + 百度OCR + SQLAlchemy

---

## 一、项目概述

**项目名称：** 企业财务智能报销与审核编排系统

**一句话定位：** 基于 CrewAI 多 Agent 编排的企业报销系统，实现发票 OCR 识别、AI 合规校验和智能审批路由。

**真实问题：**

传统企业报销流程存在以下痛点：

| 痛点 | 具体表现 |
|------|----------|
| 人工审核效率低 | 财务逐张核对发票，耗时耗力 |
| 合规检查易遗漏 | 重复报销、金额不一致难以人工发现 |
| 流程不透明 | 员工不知道报销单卡在哪个环节 |
| 数据统计困难 | 部门费用汇总依赖手工 Excel |

**目标用户：** 企业员工、财务人员、部门主管、系统管理员

---

## 二、系统架构

```
┌─────────────────────────────────────────────────┐
│                   前端 (React + Ant Design)       │
│  员工端 │ 财务端 │ 主管端 │ 管理后台 │ 数据报表    │
└───────────────────────┬─────────────────────────┘
                        │ REST API
┌───────────────────────┴─────────────────────────┐
│              后端 (FastAPI + SQLAlchemy)           │
│                                                   │
│  ┌─────────────────────────────────────────────┐ │
│  │         CrewAI 多 Agent 编排引擎              │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐    │ │
│  │  │ OCR识别   │ │ 合规校验  │ │ 审批路由  │    │ │
│  │  │ Agent    │→│ Agent    │→│ Agent    │    │ │
│  │  └──────────┘ └──────────┘ └──────────┘    │ │
│  └─────────────────────────────────────────────┘ │
│                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐         │
│  │ 百度OCR  │ │ 通知系统  │ │ 预算管理  │         │
│  └──────────┘ └──────────┘ └──────────┘         │
└───────────────────────┬─────────────────────────┘
                        │
               ┌────────┴────────┐
               │   SQLite 数据库   │
               └─────────────────┘
```

---

## 三、核心功能与当前进展

### 已完成功能

| 模块 | 功能 | 状态 |
|------|------|------|
| 发票管理 | 上传发票（JPG/PNG/PDF）、百度OCR识别、自动提取发票号/金额/日期 | ✅ |
| AI合规校验 | 真实性校验、重复报销检测、金额一致性校验、OCR置信度校验 | ✅ |
| 多级审批 | 财务初审 → 主管审批（≥5000元）→ 打款 | ✅ |
| 退单重提 | 驳回后可编辑重新提交，保留审批历史 | ✅ |
| 角色权限 | 员工/财务/主管/管理员四级权限，路由守卫 | ✅ |
| 通知系统 | 状态变更自动推送站内通知 | ✅ |
| 管理后台 | 用户管理、费用类别配置、系统参数设置 | ✅ |
| 数据报表 | 按部门/类别/时间维度统计，CSV导出 | ✅ |
| 批量操作 | 财务/主管批量审批、批量退回 | ✅ |
| 打款管理 | 财务确认打款，记录打款时间 | ✅ |
| 预算管理 | 部门预算额度设置、预算占用跟踪 | ✅ |
| 审批链配置 | 按金额/部门动态路由审批策略 | ✅ |

### 阶段性成果

- 前后端分离架构，后端 API 20+ 个接口
- 百度 OCR 真实对接，支持增值税发票和通用票据识别
- AI 校验引擎 4 项合规检查，校验结果逐条展示
- 完整的审批流程状态机（草稿→初审→主管→通过→打款）

---

## 四、AI 工具使用与系统编排

### 1. CrewAI 多 Agent 编排

系统采用 CrewAI 框架实现三个专职 Agent 的协同工作：

| Agent | 职责 | 输入 | 输出 |
|-------|------|------|------|
| OCR识别 Agent | 从发票图片提取结构化信息 | 发票图片/PDF | 发票号、金额、日期、销售方 |
| 合规校验 Agent | 执行4项合规检查 | 发票数据 + 报销单 | 校验结果（通过/警告/不通过） |
| 审批路由 Agent | 根据规则确定审批节点 | 报销金额、部门、校验结果 | 审批链路（财务/主管/管理员） |

### 2. 百度 OCR API

- **增值税发票识别** (`vat_invoice`)：提取发票代码、号码、金额、税额、销售方等
- **通用文字识别** (`general_basic`)：作为兜底方案，处理火车票、出租车票等非标发票
- **PDF 转图**：使用 PyMuPDF 将 PDF 发票转为图片后识别

### 3. AI 合规校验规则

| 检查项 | 说明 | 不通过处理 |
|--------|------|-----------|
| 发票真实性 | 发票号码格式校验、销售方信息完整性 | 阻断提交 |
| 重复报销 | 全库检索相同发票号，关联已审批/打款记录 | 阻断提交 |
| 金额一致性 | 发票含税金额与报销金额对比，差额>0.01元触发 | 警告，可提交 |
| OCR置信度 | 识别置信度<70%触发人工复核提醒 | 警告，可提交 |

---

## 五、存在的问题与风险

| 问题 | 影响 | 应对措施 |
|------|------|----------|
| OCR 对非标发票识别率有限 | 火车票、出租车票等结构化提取不完整 | 已实现通用OCR兜底 + 人工复核 |
| 百度 OCR API 调用限制 | 高并发场景可能触发限流 | 可加入本地缓存和异步队列 |
| 数据库使用 SQLite | 生产环境并发性能有限 | 可迁移至 PostgreSQL |
| 缺少电子发票验真 | 无法对接国家税务总局查验平台 | 后续可集成发票验真API |
| 无操作审计日志 | 仅记录审批日志，缺少CRUD审计 | 后续可加入全操作审计 |

---

## 六、下一步计划

1. **电子发票验真**：对接国家税务总局发票查验平台，验证发票真伪
2. **操作审计日志**：记录所有关键操作，支持追溯和合规审查
3. **差旅申请关联**：出差申请单与报销单关联，实现差旅全流程管理
4. **多币种支持**：支持外币报销，自动汇率换算
5. **数据库升级**：SQLite 迁移至 PostgreSQL，支持更高并发

---

## 七、希望评委指导的问题

1. **AI 校验准确率**：当前 OCR + 规则引擎的组合方案，如何进一步提升合规校验的准确率？是否需要引入大模型做语义理解？

2. **多 Agent 可靠性**：CrewAI 多 Agent 编排在生产环境中，如何保证任务执行的稳定性和异常恢复能力？

---

*项目负责人：___________ &emsp; 联系方式：___________ &emsp; 日期：2026年6月*
